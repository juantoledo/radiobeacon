[pytest]
# `adapters` also resolves from the editable install (-e .) in .venv; `src`
# here keeps the suite runnable straight from a checkout too.
pythonpath = src
markers =
    integration: hits real external services; excluded by default
addopts = -m "not integration"
