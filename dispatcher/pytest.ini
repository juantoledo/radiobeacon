[pytest]
# `adapters` (imported by dispatcher's submodules) resolves from the
# editable install `-e ../data-adapters` in .venv (see requirements.txt);
# `src` puts this package's own code ahead of its editable install.
pythonpath = src
