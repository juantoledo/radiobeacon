[pytest]
# `adapters` and `dispatcher` (imported by ui.db / ui.queries / the routers)
# resolve from the editable installs `-e ../data-adapters` / `-e ../dispatcher`
# in .venv (see requirements.txt).
pythonpath = src
