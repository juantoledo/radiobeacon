{
  "settings.BEACON_CALLSIGN.label": "Callsign",
  "settings.BEACON_CALLSIGN.description": "The beacon's amateur radio callsign, e.g. CD3DXZ-1.",
  "settings.BEACON_DESCRIPTION.label": "Description",
  "settings.BEACON_DESCRIPTION.description": "Longer description of the beacon/project.",
  "settings.BEACON_SHORT_DESCRIPTION.label": "Short description",
  "settings.BEACON_SHORT_DESCRIPTION.description": "One-line summary, e.g. for compact displays.",
  "settings.BEACON_OPERATOR_CONTACT.label": "Operator contact",
  "settings.BEACON_OPERATOR_CONTACT.description": "How to reach the operator (email, etc.).",
  "settings.BEACON_GRID_LOCATOR.label": "Grid locator (QTH)",
  "settings.BEACON_GRID_LOCATOR.description": "Maidenhead grid square, e.g. FF46vb.",
  "settings.BEACON_FREQUENCY.label": "Frequency",
  "settings.BEACON_FREQUENCY.description": "VHF transmit frequency, e.g. 144.390 MHz.",

  "settings.BEACON_ENABLED.label": "Enabled",
  "settings.BEACON_ENABLED.description": "Whether the beacon transmit loop actually puts scheduled content on air. Re-read every tick — no restart needed to flip it.",
  "settings.BEACON_TYPE.label": "Beacon type",
  "settings.BEACON_TYPE.description": "What each ready item is transmitted as. \"voice\" synthesizes a spoken-word WAV (piper/espeak). \"frame\" renders an AX.25 UI frame to a 1200-baud AFSK WAV via Direwolf's gen_packets, one per chunk. Either way a single WAV is produced and handed to the WAV transmitter (see Beacon — SvxLink). Changing this clears any pending rows of the other type on the next tick.",
  "settings.BEACON_TICK_SECONDS.label": "Poll interval (s)",
  "settings.BEACON_TICK_SECONDS.description": "Upper bound on how long the transmit loop waits before re-checking the schedule — newly queued content wakes it immediately, this is just the fallback poll interval.",
  "settings.BEACON_INTER_TX_DELAY_SECONDS.label": "Inter-transmission delay (s)",
  "settings.BEACON_INTER_TX_DELAY_SECONDS.description": "Gap between consecutive transmissions when draining a backlog, so PTT / the svxlink-txqueue channel-idle wait can settle between clips.",

  "settings.BEACON_WAV_TRANSMITTER.label": "WAV transmitter",
  "settings.BEACON_WAV_TRANSMITTER.description": "\"logging\" (default, safe) just logs the WAV it would hand off. \"spool\" drops the WAV into the svxlink-txqueue spool for SvxLink to play when the RF channel is idle — see documentation/svxlink-txqueue-SETUP.md.",
  "settings.BEACON_TXQUEUE_INCOMING_DIR.label": "svxlink-txqueue incoming dir",
  "settings.BEACON_TXQUEUE_INCOMING_DIR.description": "Folder the \"spool\" WAV transmitter drops rendered WAV files into — svxlink-txqueue's documented drop point, which stamps a FIFO timestamp, validates, and sanitizes the name. Must match svxlink-txqueue's TXQUEUE_SPOOL/incoming.",
  "settings.BEACON_SVXLINK_CONF_PATH.label": "svxlink.conf path",
  "settings.BEACON_SVXLINK_CONF_PATH.description": "Absolute path to svxlink.conf, used ONLY by the in-dashboard editor on this page — the beacon never reads this file itself. Blank disables the editor. In the Docker deployment this file must also be bind-mounted into the UI container and writable by its user.",
  "settings.BEACON_RF_CONF_EDITOR_ENABLED.label": "Enable svxlink.conf / direwolf.conf editor",
  "settings.BEACON_RF_CONF_EDITOR_ENABLED.description": "Allow editing svxlink.conf (this page) and direwolf.conf (the Direwolf page) from this dashboard. OFF by default: the dashboard has no login, so with this on, anyone who can reach it can read or overwrite any file the UI process can write. Only enable on a trusted host. A timestamped .bak is written on every save; no service is restarted (the page shows the systemctl command).",

  "settings.BEACON_VOICE_TEMPLATE.label": "Voice template",
  "settings.BEACON_VOICE_TEMPLATE.description": "str.format-style template wrapping resolved voice content (itself already wrapped by BEACON_VOICE_PREFIX/SUFFIX below). Placeholders: {callsign}, {text}, {date} (blank if the item has no source_date_time), plus item fields {source}, {item_id}, {type}, {subtype}, {extracted_title}, {url}. Frame content has no template — its ORIGEN>DESTINO: structure is fixed protocol code — but BEACON_FRAME_PREFIX/SUFFIX support the same placeholders. An invalid placeholder falls back to \"\" rather than crashing the transmit loop. Defaults to a bare {text} passthrough — the framing lives in the prefix/suffix below.",
  "settings.BEACON_VOICE_PREFIX.label": "Voice content prefix",
  "settings.BEACON_VOICE_PREFIX.description": "Prepended to the resolved voice text before it's substituted into BEACON_VOICE_TEMPLATE's {text} — separate from BEACON_FRAME_PREFIX, which wraps frame content instead. A str.format template — {date} and the item-field placeholders (see BEACON_VOICE_TEMPLATE) are available. Added outside BEACON_VOICE_MAX_CHARS's truncation budget, mirroring how frame prefix/suffix wrap an already-sized chunk. Defaults to a spoken-Spanish bulletin opener naming the source and the item's date.",
  "settings.BEACON_VOICE_SUFFIX.label": "Voice content suffix",
  "settings.BEACON_VOICE_SUFFIX.description": "Appended to the resolved voice text before it's substituted into BEACON_VOICE_TEMPLATE's {text}. Same placeholders and truncation-budget behavior as BEACON_VOICE_PREFIX. Defaults to a closing line pointing listeners to the source's official channels.",
  "settings.BEACON_VOICE_MAX_CHARS.label": "Voice max chars",
  "settings.BEACON_VOICE_MAX_CHARS.description": "Max characters of resolved voice text before word-boundary truncation (first piece only, no part markers — unlike frame chunking, the rest is silently dropped). A time-budget cap, not a protocol limit like frame's. Deliberately separate from ACTIONS_AI_MAX_CHARS, whose job is gating whether the LLM runs at all.",
  "settings.BEACON_DATE_FORMAT.label": "Date format",
  "settings.BEACON_DATE_FORMAT.description": "strftime format for {date} above — items.source_date_time converted to DISPLAY_TIMEZONE. Stick to fixed-width numeric directives (%d/%m/%Y/%H/%M); a weekday/month name (%A/%B) isn't accounted for by actions.chunk's AX.25 overflow-safety clamp.",

  "settings.BEACON_FRAME_DESTINATION.label": "Frame destination (tocall)",
  "settings.BEACON_FRAME_DESTINATION.description": "The AX.25 destination address — a software/project identifier, not a real route. NFO marks this as experimental, non-APRS traffic.",
  "settings.BEACON_FRAME_PREFIX.label": "Frame content prefix",
  "settings.BEACON_FRAME_PREFIX.description": "Prepended to the actual transmitted payload (not the tocall address above) — applied to every frame, including each chunk of a multi-frame item. A str.format template — {date} plus item fields {source}, {item_id}, {type}, {subtype}, {extracted_title}, {url} are available (see Beacon — Templates). actions.chunk's dynamic max-chars clamp accounts for this item's real rendered values, not just the raw template.",
  "settings.BEACON_FRAME_SUFFIX.label": "Frame content suffix",
  "settings.BEACON_FRAME_SUFFIX.description": "Appended to the actual transmitted payload — e.g. \"[EXPERIMENTAL]\" or \" {date}\". Applied to every frame, including each chunk of a multi-frame item, so a listener catching only one still sees it. Counts against the same 256-byte AX.25 hard limit as the rest of the frame — actions.chunk's dynamic max-chars clamp accounts for this item's real rendered values (same placeholders as BEACON_FRAME_PREFIX), not just this template's raw length.",

  "settings.BEACON_GEN_PACKETS_BINARY.label": "gen_packets binary",
  "settings.BEACON_GEN_PACKETS_BINARY.description": "Command used to render an AX.25 frame to an AFSK WAV when BEACON_TYPE=frame. Ships with the `direwolf` package (apt install direwolf) — no Direwolf process runs, it's invoked one-shot per frame.",
  "settings.BEACON_FRAME_LEAD_SILENCE_MS.label": "Frame lead silence (ms)",
  "settings.BEACON_FRAME_LEAD_SILENCE_MS.description": "Milliseconds of silence prepended to each rendered frame WAV so the first bits aren't clipped while SvxLink keys the transmitter.",
  "settings.BEACON_DIREWOLF_CONF_PATH.label": "direwolf.conf path",
  "settings.BEACON_DIREWOLF_CONF_PATH.description": "Absolute path to a direwolf.conf, if you run a full Direwolf instance on this host. radiobeacon itself only uses `gen_packets` and needs no direwolf.conf, so this is blank by default — blank hides the editor. Requires BEACON_RF_CONF_EDITOR_ENABLED (on the SvxLink page) to be on.",

  "settings.BEACON_TTS_ENGINE.label": "TTS engine",
  "settings.BEACON_TTS_ENGINE.description": "\"piper\" (default) is offline neural TTS — much more natural, using the voice model shipped at BEACON_TTS_PIPER_MODEL. \"espeak\" is an offline/zero-setup fallback but sounds robotic.",
  "settings.BEACON_TTS_VOICE.label": "TTS voice (espeak)",
  "settings.BEACON_TTS_VOICE.description": "espeak-ng voice/language code used to synthesize speech. Only applies when BEACON_TTS_ENGINE=espeak.",
  "settings.BEACON_TTS_PIPER_MODEL.label": "Piper voice model path",
  "settings.BEACON_TTS_PIPER_MODEL.description": "Path to a downloaded piper .onnx voice model (its .onnx.json sidecar must sit alongside it). Required when BEACON_TTS_ENGINE=piper. Defaults to the es_MX (Latin American Spanish) voice model shipped at beacon/storage/piper_voices/.",
  "settings.BEACON_TTS_PIPER_BINARY.label": "Piper binary",
  "settings.BEACON_TTS_PIPER_BINARY.description": "Command used to invoke piper. Only applies when BEACON_TTS_ENGINE=piper.",
  "settings.BEACON_TTS_WAV_DIR.label": "TTS output directory",
  "settings.BEACON_TTS_WAV_DIR.description": "Where the beacon writes synthesized WAV files before transmitting. A relative path is resolved against the repo root (not beacon/); the dashboard reads this same directory to play a bulletin's voice clip back in the browser, so in a split host/container deployment it must point at storage shared by both.",
  "settings.BEACON_MANUAL_VOICE_TEMPLATE.label": "Manual message template",
  "settings.BEACON_MANUAL_VOICE_TEMPLATE.description": "Wraps text typed into the dashboard's \"Transmit now\" action for the voice channel, so the callsign is always spoken. Placeholders: {callsign}, {text} (an invalid one falls back to \"\"). The frame channel needs no template — the callsign is already in the AX.25 header.",
  "settings.BEACON_VOICE_ATTENTION_TONE.label": "Attention tone",
  "settings.BEACON_VOICE_ATTENTION_TONE.description": "A short sequence of beeps prepended to every rendered voice WAV (scheduled bulletins and \"Transmit now\" alike — not the watermark) so listeners know an announcement is starting. Comma-separated \"freq:ms\" pairs, freq in Hz with 0 meaning silence — e.g. \"1400:250,0:120,1400:250\" is two 250 ms tones 120 ms apart. Defaults to a short rising/falling sweep; clear the field for no tone. Use the Preview button to hear it before saving.",

  "settings.BEACON_WATERMARK_ENABLED.label": "Enabled",
  "settings.BEACON_WATERMARK_ENABLED.description": "Whether the periodic watermark message transmits on its own timer, independent of item content. Re-read every tick.",
  "settings.BEACON_WATERMARK_INTERVAL_SECONDS.label": "Interval (s)",
  "settings.BEACON_WATERMARK_INTERVAL_SECONDS.description": "How often the watermark transmits. Tracked independently of item transmissions and of BEACON_TICK_SECONDS.",
  "settings.BEACON_WATERMARK_VOICE_TEMPLATE.label": "Voice template",
  "settings.BEACON_WATERMARK_VOICE_TEMPLATE.description": "str.format-style template for the watermark message when BEACON_TYPE=voice. Placeholders: {callsign}, {date}, plus every other beacon attribute (see this group's description above). Rendered through the same TTS engine/voice as regular content.",
  "settings.BEACON_WATERMARK_FRAME_TEMPLATE.label": "Frame template",
  "settings.BEACON_WATERMARK_FRAME_TEMPLATE.description": "str.format-style template for the watermark message when BEACON_TYPE=frame, wrapped as CALLSIGN>DEST:template like any other frame. Same placeholders as the voice template above. Subject to the same 256-byte AX.25 hard limit -- an over-length render is dropped and logged rather than transmitted.",

  "settings.BEACON_QUEUE_MAX_SIZE.label": "Max pending transmit rows per kind",
  "settings.BEACON_QUEUE_MAX_SIZE.description": "Max pending beacon_tx_schedule rows held per kind (voice/frame) before the oldest is dropped to make room for new content. Frame content is one row per CHUNK, not per item — a real SENAPRED report has produced 57 chunks on its own, so keep this comfortably above the largest item you expect. Applies to newly scheduled items — no restart needed.",
  "settings.BEACON_MAX_QUEUED_AGE_SECONDS.label": "Max queued age (s)",
  "settings.BEACON_MAX_QUEUED_AGE_SECONDS.description": "A scheduled transmission that has waited this long without going on air is dropped unsent (audited as beacon.tx.skipped_stale) instead of transmitted — so turning transmit off overnight doesn't dump a stale backlog on air when it comes back. Measured from the row's last enqueue/rearm/send, so a deliberate Re-transmit resets the clock. Checked every tick whether or not the beacon is enabled. \"0\" disables the check. Does not affect manual \"Transmit now\" messages. Default 21600 (6 h).",
  "settings.BEACON_CONTENT_READY_RECONCILE_INTERVAL_SECONDS.label": "Reconcile interval (s)",
  "settings.BEACON_CONTENT_READY_RECONCILE_INTERVAL_SECONDS.description": "How often beacon checks for an item.content_ready publish its own MQTT subscription missed (e.g. a message published while briefly offline, or a startup race) and enqueues it anyway. Runs once immediately on startup regardless of this interval.",

  "settings.BEACON_MQ_HOST.label": "MQTT host",
  "settings.BEACON_MQ_HOST.description": "Broker host beacon subscribes to for incoming content.",
  "settings.BEACON_MQ_PORT.label": "MQTT port",
  "settings.BEACON_MQ_PORT.description": "Broker port for beacon.",
  "settings.BEACON_MQ_QOS.label": "MQTT QoS",
  "settings.BEACON_MQ_QOS.description": "QoS level for beacon's subscriptions.",
  "settings.BEACON_MQ_RECONNECT_BACKOFF_SECONDS.label": "Reconnect backoff (s)",
  "settings.BEACON_MQ_RECONNECT_BACKOFF_SECONDS.description": "Seconds between beacon's MQTT reconnect attempts.",
  "settings.BEACON_CONTENT_READY_SUBSCRIBE_TOPIC.label": "Content-ready subscribe topic",
  "settings.BEACON_CONTENT_READY_SUBSCRIBE_TOPIC.description": "The single trigger feeding both the voice and frame queues — published by actions.content_ready once both chunk and AI have finished with an item, avoiding the race where AX.25 would send raw chunked text before an AI summary was ready.",

  "settings.BEACON_NTP_SERVER.label": "NTP server",
  "settings.BEACON_NTP_SERVER.description": "Queried for clock-offset visibility only — never used to correct the clock, which stays the OS's own NTP daemon's job.",
  "settings.BEACON_NTP_CHECK_INTERVAL_SECONDS.label": "Check interval (s)",
  "settings.BEACON_NTP_CHECK_INTERVAL_SECONDS.description": "How often to query the NTP server for the current offset.",
  "settings.BEACON_NTP_MAX_OFFSET_SECONDS.label": "Max offset before warning (s)",
  "settings.BEACON_NTP_MAX_OFFSET_SECONDS.description": "Log loudly (does not block transmission) if the measured clock offset exceeds this."
}
