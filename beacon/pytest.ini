[pytest]
# `adapters` (imported by beacon.__main__ / beacon.content / beacon.formatters)
# resolves from the editable install `-e ../data-adapters` in .venv (see
# requirements.txt).
pythonpath = src
