# -e ../data-adapters brings in `adapters` (storage, timeutil, transmit_policy,
# attention_tone, beacon_defaults, ax25, templating) and its deps, including
# tzdata — which beacon needs for zoneinfo on a slim host.
-e ../data-adapters
paho-mqtt
cloudevents>=1.9,<2
ntplib
# Only needed when BEACON_TTS_ENGINE=piper (provides the `piper` CLI binary
# voice.py shells out to) -- espeak-ng, the default engine, is a separate
# apt package, not a Python dependency.
piper-tts
pytest
